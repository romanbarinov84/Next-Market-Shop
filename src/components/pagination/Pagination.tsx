"use client"

import { PaginationProps } from "@/src/types/paginationProps";



const Pagination = ({
    totalItems,
    currentPerPage,
    basePath,
    itemsPerPage,
    searchQuery,
}: PaginationProps) => {
    return <div></div>;
};

export default Pagination;
